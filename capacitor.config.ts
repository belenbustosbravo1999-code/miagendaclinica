import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cl.tuapellido.miagendaclinica',
  appName: 'MiAgendaClinica',
  webDir: 'www'
};

export default config;
